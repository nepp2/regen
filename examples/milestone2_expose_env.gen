
;; the following two expressions should do the same thing:
;;   (def bar 5)
;;   (ccall map_insert env :bar 5)

(def foo 4)
(ccall map_insert env (sym bar) 5)

(debug foo)
(debug bar)
(debug foo + bar) ;; should print 9

