
(def main (fun () (

  (ccall env_insert env (sym bar) 5)
  (debug (ccall env_get env (sym bar)))
  (debug bar)

  ;; (let hello (sym hello))
  ;; (ccall print_symbol hello)
  ;; (debug hello)
  ;; (debug _env)
)))
