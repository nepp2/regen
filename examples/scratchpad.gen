
include(env, ref "examples/lib/prelude.gen");

reactive tick = create_timer(1000);

def seconds = tick / 1000;

def sum_fold = fun(sum : ptr i64, val : ptr i64) {
  *sum = *sum + *val;
}

reactive sum =
  create_container(onchange seconds, i64, 0, sum_fold as update_fn);
