(include env (sym macros))

(function add (a b) (
  (+ a b)
))

(do
  (let a (list64_create 93 82 93))
  (let acc 0)
  (for i 0 (list64_len a) (do
    (set acc (+ acc (list64_get a i)))
  ))
  (if (= acc 268)
    ()
    ((fail (sym ACC_VALUE_INCORRECT)))
  )
)
