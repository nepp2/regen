
;; the following two expressions should do the same thing:
;;   (def bar 5)
;;   (map_insert env :bar 5)

(def foo 4)
(env_insert env (sym bar) 5 u64)

(debug foo)
(debug bar)
(if (= (+ foo bar) 9)
  ()
  ((fail (sym EXPECTED_9))))

