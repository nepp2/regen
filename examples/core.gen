
(def butt (fun (a) (
  (block run
    (debug a)
    exit)
)))

(def main (fun () (
  (vars blah result) 
  (block condition
    (set blah 5)
    (set result (+ 0 blah))
    (cjump result then else))
  (block then
    (invoke butt 57)
    (jump continue))
  (block else
    (debug blah)
    (jump continue))
  (block continue
    exit)
)))
