
(fun () (
  (vars blah result)

  (block condition
    (set blah 5)
    (set result (+ 0 blah))
    (cjump result then else)
  )

  (block then
    (debug THEN)
    (jump continue)
  )

  (block else
    (debug ELSE)
    (jump continue)
  )

  (block continue
    exit
  )
))

