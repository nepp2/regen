
// TODO: is there an example that requires more than one def in the graph?

def add[A, B] = {
  fun(a : A, b : B) {
    a + b
  }
};

def list[T] = {
  struct { data : ptr T, length : i64 }
};

def list_new[T] = {
  fun() => list(T) {
    // blah blah logic
  }
};

def list_add[ListType, T] = {
  assert(ListType == list(T));
  fun(l : list(T), v : ValueType) {
    // blah blah logic
  }
};

let xs = list_new[i64]();
list_add(xs, 5);
